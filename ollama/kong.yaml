_format_version: '3.0'
_transform: true

services:
  - name: ollama
    url: http://ollama:11434
    routes:
      - name: http
        paths:
          - /
    plugins:
      - name: key-auth
#      - name: basic-auth

consumers:
  - username: kong-user
    keyauth_credentials:
      - key: KEY_AUTH_KEY
#    basicauth_credentials:
#      - username: kong
#        password: BASIC_AUTH_PASSWORD