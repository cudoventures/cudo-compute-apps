_format_version: '3.0'
_transform: true

services:
  - name: jupyter
    url: http://jupyter:8888
    routes:
      - name: http
        paths:
          - /
#        headers:
#          upgrade:
#            - WebSocket
#          connection:
#            - Upgrade
#    plugins:
#      - name: key-auth
#      - name: basic-auth

consumers:
  - username: kong-user
#    keyauth_credentials:
#      - key: my-key
    basicauth_credentials:
      - username: kong
        password: BASIC_AUTH_PASSWORD